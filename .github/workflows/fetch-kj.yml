name: Fetch KJ
on:
  schedule:
    - cron: '17 * * * *'
  workflow_dispatch:

jobs:
  fetch:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      - run: curl -s "https://2026kj.xswqa.app/kjapi/index/index/kjdata?g=am" -o kj.json
      - run: |
          git config user.name "cater12478"
          git config user.email "928865989@qq.com"
          git add kj.json
          git commit -m "update" || exit 0
          git push
